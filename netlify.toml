[build]
  publish = "portal"
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"
  included_files = ["netlify/functions/**"]

# Scheduled function - polls LinkedIn every 10 minutes
[[plugins]]
  package = "@netlify/plugin-scheduled-functions"
